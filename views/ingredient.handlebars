<link rel="stylesheet" type="text/css" href="/css/ingredient.css">
<h4>
  Welcome back {{user.name}} !
</h4>

<div class="content">
  <ul class="rounded-list">
    {{#each dbitems as |item|}}
        {{#if item.name}}
        <li>
        <span>
          <button class="deleteIngredients" value='{{item.id}}'>{{item.name}}</button>
          </span>
      </li>
        {{/if}}

    {{/each}}
  </ul>

<form action='/ingredient/sendSms' method=post>
  <label>Send list to mobile number +65: <input type="text" name="hp"></label>
  <button id='sms'><i class="material-icons">perm_phone_msg</i></button>
</form>
  <p class="disclaim">Note: Only mobile numbers verified with <a href="mailto:ngwanzhen@gmail.com">Admin</a> will be able to receive the messages.

</div>
